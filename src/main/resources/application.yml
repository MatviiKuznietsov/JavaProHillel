spring:
  datasource:
    url: jdbc:mysql://localhost:8080/OrdersDataBase
    username: OrderDB
    password: 0123456789
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

logging:
  level:
    root: INFO
    org.springframework.web: INFO
  file: logs/application.log